---
name: process-issue
description: GitHub 이슈 번호를 받아 코드 변경·PR·독립 리뷰와 사람 인계까지 처리하는 5역할(설계자·개발자·테스터·보고자·리뷰어) 팀 워크플로. 이슈를 파악해 목표로 분해하고, 작업 유형·분량·난이도에 따라 서브에이전트 또는 에이전트 팀을 골라 개발→검증→PR→리뷰(pr-review 기준)를 수행한다. "이슈 N번 처리/작업/해결해줘", "N번 이슈 개발", "깃헙 이슈 반영", "PR까지", 그리고 "다시/이어서/수정/보완" 같은 후속 요청 시 반드시 이 스킬을 사용할 것. 단, 이슈를 단순 조회·요약만 요청하면 직접 답하고(파이프라인 미가동), 이슈와 무관한 단독 PR 리뷰는 crefle-agent-skills 의 pr-review, 완성된 HTML 리포트 등록은 register-report 를 쓴다.
---

# 이슈 처리 팀 (process-issue)

GitHub 이슈 하나를 받아 **개발 → 검증 → PR → 리뷰 → 사람 인계** 까지 끝내는 오케스트레이터다.
다섯 역할이 협업한다: **설계자**(파악·분해·조율·책임) · **개발자**(구현) · **테스터**(검증) ·
**보고자**(PR) · **리뷰어**(pr-review 기준 리뷰·머지 판정).

**메인 세션이 설계자(리더)를 맡는다.** 개발자·테스터·보고자·리뷰어는 `Agent` 도구로 스폰하고,
공유 작업 보드(`TaskCreate`)와 `SendMessage`로 조율한다. 각 역할 정의는 `.claude/agents/issue-*.md`.

## 언제 / 언제 아닌가

- **사용**: "6번 이슈 처리해줘", "이슈 #3 개발해서 PR 올려줘", "이 이슈 반영", 그리고 후속("다시", "이어서", "수정").
- **사용 안 함**:
  - 이슈를 **조회·요약만** 원할 때 → 파이프라인 없이 직접 답한다(`gh issue view`).
  - **완성된 HTML 리포트 등록** → `register-report` 스킬(별개 하네스).

## 입력

- **이슈 번호(필수).** 없으면 되묻는다. 저장소는 기본 현재 repo(`CREFLEINC/reports`).
- 끝점은 **리뷰 후 사람 인계**다. 리뷰어가 pr-review 승인 기준(Blocker·Major 0)과 안전
  조건(CI green·충돌 없음·base main·Draft 아님)을 확인해 머지 가능 여부를 판정하고,
  결과와 무관하게 PR을 직접 머지하지 않고 열린 채 사용자에게 넘긴다.

## 모델 배정

| 역할 | 에이전트 | 모델 | subagent_type |
|------|----------|------|---------------|
| 설계자 | (메인 세션) `issue-architect` | Opus 4.8 | — |
| 개발자 | `issue-developer` | **opus** | general-purpose |
| 테스터 | `issue-tester` | **sonnet** | general-purpose |
| 보고자 | `issue-reporter` | **sonnet** | general-purpose |
| 리뷰어 | `issue-reviewer` | **opus** | general-purpose |

> 사용자 지정 배정이다. 하네스 기본값("전부 opus")을 의도적으로 오버라이드한다. `Agent` 호출마다
> `model` 을 위 표대로 명시한다. 검증 스크립트 실행이 필요하므로 서브타입은 `general-purpose`
> (`Explore`는 읽기 전용이라 부적합). 리뷰어는 머지 게이트이므로 opus.

## 팀 표준 연계 (crefle-agent-skills 플러그인)

CREFLE 팀 표준 스킬 3종과 연동한다(2026-07-24). 서브에이전트는 플러그인 스킬 파일을 직접
읽는다: `find ~/.claude/plugins/cache -path "*crefle-agent-skills*" -name "SKILL.md"`.

| 팀 스킬 | 연결 지점 |
|---------|----------|
| `coding-rules` | 개발자: 코드 작성 전 공통 원칙 + 언어별 규칙 확인. 보고자: 커밋·PR 제목 Conventional Commits. 리뷰어: 컨벤션 영역 대조 기준. |
| `issue-management` | 라벨 동기화 — 착수 시 `status:in-progress`(start_issue.sh 자동), PR 생성 시 `status:in-review`(보고자). 사람의 머지 전까지 `status:in-review`를 유지. |
| `pr-review` | 리뷰어가 4영역 점검·심각도 분류·리뷰 코멘트 게시·사람의 머지 가능 여부 판정 기준으로 사용. |

- **브랜치 규칙**: 팀 표준 `<type>/<설명>` 에 이슈 추적용 번호를 더한 `<type>/<슬러그>-<번호>`
  (예: `feat/rest-api-token-19`). `start_issue.sh` 가 이슈 라벨·제목에서 type 을 추론해 생성한다.
- **폴백**: 플러그인 미설치 환경에서는 라벨 갱신은 조용히 생략되고(스크립트가 실패 무시),
  리뷰어는 일반 기준으로 리뷰만 하고 **머지하지 않는다**. 파이프라인 자체는 멈추지 않는다.

## Phase 0: 컨텍스트 확인

`_workspace/issue-<n>/` 존재 여부로 실행 모드를 가른다.

- 없음 → **초기 실행** (아래 Phase 1부터).
- 있음 + 사용자가 부분 수정 요청 → **부분 재실행** (해당 조각의 개발자/테스터만 재호출).
- 있음 + 새 방향 지시 → 기존을 `_workspace/issue-<n>_prev/`로 옮기고 **새 실행**.

## Phase 1: 설계자 트리아지 & 모드 선택

메인 세션(설계자)이 수행한다.

1. **착수**: `scripts/start_issue.sh <n>` — 이슈 조회 + type 추론 브랜치(`<type>/<슬러그>-<번호>`,
   base: 최신 `origin/main`) 생성 + 이슈 라벨 `status:in-progress` 갱신. 본문·코멘트·연결 PR을
   읽는다. 관련 코드도 확인한다.
2. **수용 기준 도출**: 이슈가 "무엇이 되면 끝인가"를 검증 가능한 항목으로 뽑는다. 모호하면
   착수 전에 사용자에게 되묻는다(추측 금지).
3. **모드 선택** — 아래 루브릭:

   | 신호 | 서브에이전트(단순) | 에이전트 팀(복잡) |
   |------|------------------|-----------------|
   | 변경 파일 수 | ~1–2 | 3+ 또는 여러 모듈 |
   | 딜리버러블 수 | 1 | 2+ (이슈에 기능 여러 개) |
   | 난이도·위험 | 낮음(국소·명확) | 높음(런타임·CI·데이터 영향) |
   | 분해 필요성 | 불필요 | 필요 |

   - **문서/한 줄/설정** 수준(trivial): 설계자가 직접 처리하고 테스터만 검증.
   - **단순**: 서브에이전트 패스트패스(Phase 2A).
   - **복잡**: 에이전트 팀(Phase 2B).
4. **업무 지시서 작성**: 각 조각을 `references/work-order-contract.md` 형식으로 만든다.
   목표·범위·수용기준·대상파일·비목표·검증힌트를 못박아 개발자가 추측하지 않게 한다.
   복잡 이슈는 **파일이 겹치지 않는 조각**으로 나눈다.
5. 계획을 `_workspace/issue-<n>/01_architect_plan.md`에 저장한다.

## Phase 2A: 실행 — 서브에이전트 (단순)

순차 파이프라인. 팀 통신 오버헤드 없이 결과만 주고받는다.

1. **개발자**(opus) 스폰 — 업무 지시서를 프롬프트로 전달. 완료 보고를 받는다.
   ```
   Agent(subagent_type="general-purpose", model="opus",
         prompt="너는 issue-developer 다. .claude/agents/issue-developer.md 를 읽고 따르라.
                 업무 지시서: <work-order>. 산출물은 _workspace/issue-<n>/02_dev_main_report.md.")
   ```
2. **테스터**(sonnet) 스폰 — 수용 기준 + 개발자 보고를 전달, PASS/FAIL 판정을 받는다.
3. **검증 루프**(Phase 3)로 이동.

## Phase 2B: 실행 — 에이전트 팀 (복잡)

1. **작업 보드 구성**: 조각마다 `TaskCreate`(의존관계 포함).
2. **개발자 병렬 스폰**(opus, `run_in_background: true`): 각자 **겹치지 않는 파일 스코프**의
   조각 1건. 스코프가 겹칠 위험이면 순차로 조정하거나 `isolation: "worktree"`.
3. 각 개발자 완료 즉시 **테스터**(sonnet)가 그 조각을 **점진 검증**한다(전체 완성 후 1회 금지).
4. 설계자는 작업 보드로 상태를 추적하고, FAIL은 `SendMessage`로 원 개발자에게 되돌린다.
5. 모든 조각 PASS면 통합 상태에서 회귀 검증(전체 pytest) 후 Phase 4.

## Phase 3: 검증 루프

테스터는 `references/verification-playbook.md`의 방법론(수용 기준 대조 → 단위 → 실제 실행 → 증거)으로 판정한다.

- 테스터 판정이 **FAIL**: 결함(어떤 기준·어떤 입력·어떻게 어긋남)을 개발자에게 되돌려 수정 → 재검증.
- 같은 조각 **2회 연속 실패**: 설계자가 목표를 더 잘게 쪼개거나 접근을 바꿔 재지시. 3회면 사용자에게 판단 요청.
- 전체 조각 PASS + 회귀 없음 → Phase 4.

## Phase 4: 정리 & PR

1. **보고자**(sonnet) 스폰 — `_workspace/issue-<n>/`의 계획·개발·판정 산출물을 근거로:
   커밋 정리(Conventional Commits) → PR 본문 작성(`Closes #n`, 무엇을·왜, 조각별 변경, 검증 근거,
   비목표) → `main` 기준 PR 생성(제목도 `<type>(<scope>): ...` — Squash 시 최종 커밋 메시지가 된다)
   → 이슈 라벨 `status:in-review` 갱신.
2. **설계자 검토**: 보고자가 반환한 PR이 이슈 수용 기준을 충족하는지 확인한다. 미흡하면
   보고자에게 보완을 지시한다. 충족하면 Phase 5(리뷰)로 넘긴다.

> "설계자가 내용을 정리하여 PR을 생성한다"는 요구의 해석: 보고자가 PR 작성·게시를 수행하고,
> 설계자가 전체를 총괄해 최종 PR을 성립시킨다(책임 소재는 설계자).

## Phase 5: 리뷰 & 사람 인계

1. **리뷰어**(opus) 스폰 — PR 번호·이슈 번호·`_workspace/issue-<n>/` 경로를 전달. 리뷰어는
   `crefle-agent-skills:pr-review` 절차로 4영역 점검 → 심각도 분류 → 한국어 리뷰 코멘트를 PR 에
   게시 → 머지 판정. 판정서는 `05_reviewer_verdict.md`.
2. **분기**:
   - **머지 가능**: 조건 전부 충족 → 열린 PR을 유지하고 설계자가 사용자에게 PR URL·리뷰 요약·안전 조건을 보고한다.
   - **Blocker/Major 발견**: 설계자가 결함을 개발자에게 반려 → 수정 → 테스터 재검증 →
     보고자 커밋 추가 → 리뷰어 재판정(델타 리뷰). 이 루프는 **1회**만 — 재실패 시 PR 을 열어둔
     채 사용자에게 판단을 넘긴다.
   - **머지 보류**(CI 미완·충돌·불확실): PR 과 리뷰 코멘트를 남긴 채 보류 사유와 함께 사용자에게
     보고한다. 하네스가 임의로 재시도하지 않는다.

## 데이터 전달 프로토콜

- **작업 보드**(`TaskCreate`/`TaskUpdate`): 조각 상태·의존관계 추적(팀 모드).
- **파일 기반**(`_workspace/issue-<n>/`): 산출물. 컨벤션 `{순번}_{역할}_{조각}.md` — 예
  `01_architect_plan.md`, `02_dev_api_report.md`, `03_tester_api_verdict.md`, `04_reporter_pr.md`,
  `05_reviewer_verdict.md`.
  중간 산출물은 보존(감사 추적), 코드는 작업 트리에 직접.
- **메시지**(`SendMessage`): 스폰된 에이전트를 컨텍스트 유지한 채 이어 호출(결함 반려·재작업).

## 에러 핸들링

- 에이전트 1회 재시도 후 재실패 → 해당 산출 없이 진행하되 최종 보고에 **누락을 명시**한다.
- 상충하는 결과는 삭제하지 않고 **출처를 병기**한다.
- 검증 환경 불가(내부망·docker 없음)로 테스터가 일부만 검증 → 판정서·PR에 **미검증 항목을 명시**한다(통과로 위장 금지).
- 이슈 목표가 끝내 모호 → 파이프라인을 멈추고 사용자에게 구체 질문.
- 하네스와 모든 역할 에이전트는 `gh pr merge` 등 머지 명령을 실행하지 않는다. 승인 기준·안전
  조건을 판정한 뒤 열린 PR을 사람에게 인계한다.

## 테스트 시나리오

- **정상(단순)**: "이슈 #3 처리" → 트리아지=단순 → 개발자 1 → 테스터 PASS → 보고자 PR 생성 →
  설계자 검토 → 리뷰어 리뷰(Blocker/Major 0 + CI green) → 열린 PR을 사람에게 인계 →
  URL·리뷰 판정 보고. `.deployed_sha`·서버 등 운영은 건드리지 않음.
- **정상(복잡)**: 다기능 이슈(예 #6류) → 조각 3개 분해 → 병렬 개발자(opus, 겹치지 않는 파일) →
  조각별 점진 검증 → 전체 회귀 → 통합 PR → 리뷰 → 사람 인계.
- **에러(검증 FAIL)**: 테스터가 수용기준 2번 FAIL → 결함을 개발자에 반려 → 수정 → 재검증 PASS → 진행.
- **에러(리뷰 Major)**: 리뷰어가 Major 1건 발견 → 설계자가 개발자에 반려 → 수정 →
  재검증 → 리뷰어 델타 재판정 → 결과와 열린 PR을 사용자에게 인계. 2회째 실패면 사용자에게.
- **에러(머지 보류)**: CI 미완료/충돌 → 머지하지 않고 리뷰 코멘트 + 보류 사유 보고(PR 은 열린 채).
- **에러(모호)**: 이슈에 수용 기준을 세울 수 없음 → 개발 착수 없이 사용자에게 질문.
