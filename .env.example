# hulk 배포 위치(/home/hulk/working/reporter.crefle.com)에 .env 로 복사해 사용.
# .env 는 이미지에 굽히지 않고 docker compose 가 런타임에 주입한다.
REPORTS_USER=crefle
# 내부망·테스트용 기본값. 보안 강화가 필요하면 강한 값으로 변경 후 `docker compose up -d`.
REPORTS_PASS=crefle
